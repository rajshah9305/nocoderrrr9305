# Backend
SECRET_KEY=change-me
DATABASE_URL=sqlite:///app.db
CORS_ORIGINS=http://localhost:5173,https://your-frontend-domain

# Model keys (set only in backend env, never commit real keys)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
CEREBRAS_API_KEY=

# Frontend (Vite requires VITE_ prefix to expose at build time)
VITE_APP_NAME=AI App Builder Pro
